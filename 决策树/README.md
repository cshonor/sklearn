# 决策树

本目录为决策树相关示例与笔记。

## 示例

- **decision_tree_animals.py**：教材示例——根据动物特征（体温、表皮覆盖、胎生等）分类为哺乳类 / 非哺乳类。

运行：

```bash
python decision_tree_animals.py
```

（需先安装依赖：`pip install -r ../requirements.txt`，若使用 pandas 则需 `pip install pandas`。）
